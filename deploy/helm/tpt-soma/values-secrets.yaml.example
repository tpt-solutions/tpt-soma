# Copy this file to values-secrets.yaml and fill in real values.
# values-secrets.yaml is gitignored.
# For production, use sealed-secrets or SOPS instead of plain YAML.

keystone:
  auth:
    bootstrapPassword: "REPLACE_ME"

minio:
  rootPassword: "REPLACE_ME"
