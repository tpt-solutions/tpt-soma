[package]
name = "tpt-soma-sandbox"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.0"
license.workspace = true
authors.workspace = true
description = "Capability-gated host API surface for researcher-submitted compute (WASM sandbox)."
readme = "README.md"
keywords = ['sandbox', 'wasm', 'capability', 'compute']
categories = ['cryptography']
repository.workspace = true
homepage.workspace = true

[dependencies]
tpt-soma-capability = { path = "../tpt-soma-capability", version = "0.1.0" }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
